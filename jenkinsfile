pipeline {
    agent any
    tools {
        nodejs 'NodeJS'
    }
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/ChiranjeeviHeggade/Cypress-Cucumber-E2E.git'
            }
        }
        stage('Install Dependencies') {
            steps {
                bat 'npm install'
            }
        }
        stage('Run Smoke Tests') {
            steps {
                ansiColor('xterm') {
                    bat 'npm run SMOKE-CUCUMBER-CHROME'
                }
            }
        }
        stage('Fix Report Base Path') {
            steps {
                script {
                    def baseHref = "${env.BUILD_URL}artifact/cucumber-html-reports/"
                    def indexFile = "cucumber-html-reports\\index.html"  // Windows-style path
                    if (fileExists(indexFile)) {
                        echo "✅ Found index.html. Attempting to inject <base> tag..."
                        def html = readFile(indexFile)
                        if (!html.contains('<base href=')) {
                            html = html.replaceFirst(/<head>/, "<head><base href='${baseHref}'>")
                            writeFile file: indexFile, text: html
                            echo "✅ Successfully injected <base> tag into HTML report."
                        } else {
                            echo "ℹ️ HTML already contains <base> tag."
                        }
                        // Optional debug: print first few lines of updated HTML
                        def updatedHtml = readFile(indexFile)
                        echo "🔍 First 200 characters of updated index.html:\n" 
                            + updatedHtml.take(200)
                    } else {
                        echo "❌ index.html not found at path: ${indexFile}"
                    }
                }
            }
        }
        stage('Archive Cucumber Report') {
            steps {
                archiveArtifacts artifacts: 'cucumber-html-reports/**', fingerprint: true
            }
        }
    }
    post {
        success {
            script {
                slackSend(
                    channel: '#all-jenkins-test-reports',
                    color: 'good',
                    message: "✅ Smoke Tests Passed — ${env.JOB_NAME} #${env.BUILD_NUMBER}\n" 
                        + "📄 Report: <${env.BUILD_URL}artifact/cucumber-html-reports/index.html|Click here to see the Cucumber Report>"
                )
            }
        }
        failure {
            script {
                slackSend(
                    channel: '#all-jenkins-test-reports',
                    color: 'danger',
                    message: "❌ Smoke Tests Failed — ${env.JOB_NAME} #${env.BUILD_NUMBER}"
                )
            }
        }
    }
}
